import React from 'react';
import { StyleSheet, View } from 'react-native';

interface IStatsBar {
    value: number;
    color: string;
}

const StatsBar = ({ value, color }: IStatsBar) => {
    return (
        <View>

        </View>
    );
}

const styles = StyleSheet.create({})

export default StatsBar;
